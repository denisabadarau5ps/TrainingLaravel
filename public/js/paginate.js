(function($){
    $(document).ready(function(){
        $.fn.customPagination = function(options){
            var paginationContainer;
            var itemToPaginate;
          var def = {
              itemsPerPage : 5
            };
        };

    });
}(jQuery));
